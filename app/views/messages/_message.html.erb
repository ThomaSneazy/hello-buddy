<div class="regular cont-<%=message.user.id%>">
  <div class="message-box msg-<%= message.user.id %> bubble recipient <%= cycle("first", "middle","last") %>">

      <small>
          <strong><%= message.user.first_name.capitalize %></strong>
          <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
        </small>
        <p><%= message.content %></p>
  </div>
</div>
